<div class="post-main">
    <% page.posts.each(function (post) { %>
        <% if (post.index == true) { %>
            <div class="post-md">
                <%- post.content %>
            </div>
        <% } %> 
    <% }) %>
</div>