<% if(theme.imageShow.enable) { %>
    <style>
        body{
            background-image: url(<%= theme.imageShow.backGround %>);
        }
        .paper {
            background-image: url(<%= theme.imageShow.backGround %>);
        }  
    </style>
<% } %>